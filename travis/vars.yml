- goos: linux
  goarch: '386'
  ldflags:

- goos: linux
  goarch: 'amd64'
  ldflags: '-linkmode external -extldflags -static -s -w'

- goos: linux
  goarch: 'arm'
  ldflags:

- goos: linux
  goarch: 'arm64'
  ldflags:

- goos: darwin
  goarch: '386'
  ldflags:

- goos: darwin
  goarch: 'amd64'
  ldflags:

- goos: darwin
  goarch: 'arm'
  ldflags:

- goos: darwin
  goarch: 'arm64'
  ldflags:

- goos: windows
  goarch: '386'
  ldflags:

- goos: windows
  goarch: 'amd64'
  ldflags:
